main() {
  int num1 = 100;

  // dart에는 null을 수용하지 않겠다고 선언 할 수가 있다.
  // 이 때 강제로 null 이 들어갈 수 있게 바꿔줄 수 있다.
  int? num2 = 10;

  // num2가 null 이면 0으로 바꿔주는 것이다.
  // null 병합 연산자

  // if switch 문 java랑 똑같다.
  print(num2);
}
